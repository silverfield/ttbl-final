/* This file is here only so that files with templates are compiled first.
 *
 * The problem is that template headers must contain directly implementation, thus
 * any changes in them will invoke re-compilation of dependent modules.*/

#include "objects/graph.h"
#include "algorithms.h"
